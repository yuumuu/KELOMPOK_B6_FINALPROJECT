<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #0f172a;
      color: #f8fafc;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      text-align: center;
    }

    .container {
      padding: 3rem;
      background-color: #1e293b;
      border-radius: 1.5rem;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
      border: 1px solid #334155;
      max-width: 500px;
      width: 90%;
    }

    h1 {
      color: #38bdf8;
      font-size: 2.5rem;
      margin-bottom: 0.5rem;
      background: linear-gradient(to right, #38bdf8, #818cf8);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    p {
      color: #94a3b8;
      font-size: 1.1rem;
      line-height: 1.6;
    }

    .status-group {
      margin-top: 2rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .badge {
      padding: 0.6rem 1.2rem;
      border-radius: 0.75rem;
      font-weight: 600;
      font-size: 0.9rem;
      display: inline-block;
    }

    .badge-app {
      background-color: #064e3b;
      color: #34d399;
    }

    .badge-db {
      background-color: <%=db_status.includes('Connected') ? '#064e3b': '#7f1d1d' %>;
      color: <%=db_status.includes('Connected') ? '#34d399': '#fca5a5' %>;
    }

    .footer {
      margin-top: 2rem;
      font-size: 0.8rem;
      color: #475569;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>
      <%= title %>
    </h1>
    <p>Your professional Express.js stack with Docker isolation, Nginx reverse proxy, and MySQL persistence.</p>

    <div class="status-group">
      <div>
        <span class="badge badge-app">APP STATUS: RUNNING</span>
      </div>
      <div>
        <span class="badge badge-db">DB STATUS: <%= db_status %></span>
      </div>
    </div>

    <div class="footer">
      Built with ❤️ for Group B6 Final Project
    </div>
  </div>
</body>

</html>