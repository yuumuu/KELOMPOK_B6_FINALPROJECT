<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= typeof title !=='undefined' ? title : 'Mini Twitter' %>
  </title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Opsional: Menghilangkan scrollbar default agar lebih bersih seperti aplikasi */
    body {
      scrollbar-width: none;
      -ms-overflow-style: none;
    }

    body::-webkit-scrollbar {
      display: none;
    }
  </style>
</head>

<body class="bg-black text-white antialiased min-h-screen font-sans selection:bg-neutral-700 selection:text-white">

  <div
    class="max-w-xl mx-auto min-h-screen border-x border-neutral-800 bg-black relative shadow-2xl shadow-neutral-900">

    <header
      class="sticky top-0 z-50 bg-black/80 backdrop-blur-md border-b border-neutral-800 flex justify-between items-center px-4 py-3"
      onclick="window.scrollTo({top: 0, behavior: 'smooth'});">

      <div class="flex items-center gap-4">
        <% if (typeof title !=='undefined' && title !=='Home' && title !=='Login' && title !=='Register' ) { %>
          <a href="/" class="p-2 -ml-2 rounded-full hover:bg-neutral-900 transition text-neutral-300 hover:text-white">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18">
              </path>
            </svg>
          </a>
          <% } %>
            <h1 class="text-lg font-bold tracking-wide text-white">
              <%= typeof title !=='undefined' ? title : 'Mini Twitter' %>
            </h1>
      </div>

      <div class="flex items-center gap-4">
        <% if (user) { %>
          <form action="/logout" method="POST" class="flex items-center gap-3">
            <span class="text-xs text-neutral-500 hidden sm:block">@<%= user.username %></span>
            <button type="submit"
              class="text-xs font-bold text-red-500 hover:text-red-400 transition border border-red-500/30 px-3 py-1.5 rounded-full">
              Logout
            </button>
          </form>
          <% } else { %>
            <% if (title !=='Login' && title !=='Register' ) { %>
              <a href="/login"
                class="text-xs font-bold text-white bg-neutral-800 px-4 py-2 rounded-full hover:bg-neutral-700 transition">
                Login
              </a>
              <% } %>
                <% } %>
      </div>
    </header>

    <main class="">